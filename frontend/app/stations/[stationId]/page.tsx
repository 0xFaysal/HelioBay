import { notFound } from "next/navigation";
import Link from "next/link";
import { MapPin, ArrowUpRight, Sun, BatteryMedium, Zap, Coffee, Wifi, ShieldCheck } from "lucide-react";
import { stationService } from "@/lib/services/stations";
import { PublicShell } from "@/components/shared/public-shell";
import { AssetImage } from "@/components/shared/asset-image";
import { BookingForm } from "@/components/stations/booking-form";
type Props = { params: Promise<{stationId:string}> };
export async function generateMetadata({params}:Props) { const s=stationService.get((await params).stationId);return {title:s?.name??"Station not found",description:s?`${s.name} · ${s.address} · ${s.power} kW charging. Reserve a solar-powered bay.`:undefined,openGraph:{title:s?.name,description:s?.address,images:s?[s.image]:[]},twitter:{title:s?.name,images:s?[s.image]:[]}};}
export default async function Page({params}:Props) { const station=stationService.get((await params).stationId);if(!station)notFound();return <PublicShell><div className="container-wide"><div className="page-intro"><Link href="/stations" className="text-xs muted">← Back to all stations</Link><h1>{station.name}</h1><p className="flex gap-2 items-center"><MapPin size={15}/>{station.address}</p></div><div className="detail-layout"><div><div className="detail-photo"><AssetImage src={station.image} alt={`${station.name} architectural concept`} fill priority sizes="(max-width:850px)100vw,60vw"/><span className="absolute bottom-4 left-4 text-[10px] bg-white rounded-md px-3 py-2">Concept imagery · Demo station</span></div><div className="flex flex-wrap justify-between items-center gap-3 my-6"><span className="text-xs"><span className={`status-dot ${!station.online?"!bg-red-500":""}`}/>{station.online ? "Station online" : "Temporarily offline"} · Open 24 hours</span><a className="text-xs flex gap-1 items-center underline" href={`https://www.google.com/maps/dir/?api=1&destination=${station.lat},${station.lng}`} target="_blank" rel="noreferrer">Get directions <ArrowUpRight size={13}/></a></div><div className="grid grid-cols-3 gap-3 mb-7">{[{icon:Sun,label:"Solar contribution",value:`${station.solar}%`},{icon:BatteryMedium,label:"Station battery",value:`${station.battery}%`},{icon:Zap,label:"Max. output",value:`${station.power} kW`}].map(m=><div className="panel !p-4" key={m.label}><m.icon size={20} className="text-green-700"/><div className="text-2xl mt-3 tracking-tight">{m.value}</div><p className="text-[10px] muted">{m.label}</p></div>)}</div><h2 className="text-xl">A place to pause. Power to go.</h2><p className="text-sm muted my-4">Conveniently located near {station.landmark}, this HelioBay concept brings clean energy to your everyday route. Solar-first charging, comfortable amenities, and a bay you can count on.</p><div className="data-row"><span>Connector</span><span>{station.connector} · Up to {station.power} kW</span></div><div className="data-row"><span>Grid backup</span><span>Available · Automatic failover</span></div><div className="data-row"><span>Device</span><span>{station.deviceId}</span></div><div className="data-row"><span>Solar production (demo)</span><span>{(station.power*station.solar/100).toFixed(1)} kW</span></div><h3 className="text-lg mt-7 mb-4">Live bay availability</h3><div className="flex gap-2 flex-wrap">{Array.from({length:station.bays},(_,i)=><span className={`px-4 py-3 rounded-lg text-xs border ${i<station.available&&station.online?"bg-green-50 text-green-800":"bg-muted muted"}`} key={i}>BAY{String(i+1).padStart(2,"0")} · {!station.online ? "Offline" : i<station.available?"Available":"Occupied"}</span>)}</div><p className="text-[10px] muted mt-3">Demo availability. Your chosen time is rechecked when payment completes.</p><h3 className="text-lg mt-7 mb-4">While you recharge</h3><div className="flex flex-wrap gap-4 text-xs muted">{station.amenities.map((a,i)=>{const Icon=[Wifi,Coffee,ShieldCheck][i%3];return <span className="flex items-center gap-2" key={a}><Icon size={16}/>{a}</span>;})}</div></div><BookingForm station={station}/></div></div></PublicShell>; }
