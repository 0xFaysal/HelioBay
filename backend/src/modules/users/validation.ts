import { z } from 'zod';
export const profilePatch = z.object({ name: z.string().trim().min(1).max(100).optional(), phone: z.string().trim().max(30).nullable().optional(), city: z.string().trim().max(100).nullable().optional(), preferences:z.object({charging:z.boolean(),wallet:z.boolean(),offers:z.boolean()}).strict().optional(), savedStations:z.array(z.string().min(1).max(128)).max(200).optional() }).strict().refine(v => Object.keys(v).length > 0, 'At least one field is required');
export const vehicleInput = z.object({ name: z.string().trim().min(1).max(100), plate: z.string().trim().min(1).max(30), connectorType: z.enum(['CCS2','TYPE_2','CHADEMO','AC_SOCKET']), capacityWh: z.number().int().positive().max(1000000), estimatedSocPct:z.number().int().min(0).max(100).optional(), isDefault: z.boolean().default(false) }).strict();
export const vehiclePatch = vehicleInput.partial().extend({ isDefault: z.boolean().optional() }).refine(v => Object.keys(v).length > 0, 'At least one field is required');

